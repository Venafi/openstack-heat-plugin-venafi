# http://docs.openstack.org/developer/heat/template_guide/hot_spec.html#heat-template-version
heat_template_version: 2014-10-16


description: >
   Venafi testing certificate

parameters:
  common_name:
    type: string
    label: Common Name
    description: Name of key-pair to be used for compute instance
    default: 'fake.cert.example.com'
  tpp_user:
    type: string
    default: ""
  tpp_password:
    type: string
    default: ""
  venafi_url:
    type: string
    default: ""
  fake:
    type: boolean
    default: "false"

resources:
  fake_certificate:
    type: OS::Nova::VenafiCertificate
    properties:
      name: 'venafi certificate'
      zone: 'fake'
      common_name: { get_param: common_name }
      venafi_url: { get_param: venafi_url }
      tpp_user: { get_param: tpp_user }
      tpp_password: { get_param: tpp_password }
      fake: { get_param: fake }

outputs:
  fake_certificate:
    value: { get_attr: [fake_certificate, certificate] }
  fake_chain:
    value: { get_attr: [fake_certificate, chain] }
  fake_pkey:
    value: { get_attr: [fake_certificate, private_key] }